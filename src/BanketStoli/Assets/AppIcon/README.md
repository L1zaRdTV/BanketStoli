# Application icon

Put the application icon here when it is ready, for example:

`Assets/AppIcon/app.ico`

After adding the icon file, set it as the WPF application icon in the project properties or add `<ApplicationIcon>Assets\AppIcon\app.ico</ApplicationIcon>` to the project file.
